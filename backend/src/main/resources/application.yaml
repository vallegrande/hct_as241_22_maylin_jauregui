spring:
  application:
    name: Hackathon
  
  datasource:
    url: jdbc:postgresql://ep-odd-grass-a5zs4hqj-pooler.us-east-2.aws.neon.tech/Hackathon?sslmode=require&channel_binding=require
    driver-class-name: org.postgresql.Driver
    username: NeonDB_owner
    password: npg_z3LUOmRcrI5G
  
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8090